<!-- Video -->
<section class="video-wrapper video section-padding bg-img bg-fixed" data-overlay-dark="4" data-background="{{ setting_image('shared.video.background') }}">
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-center">
                <div class="section-subtitle">{{ setting('shared.video.subtitle') }}</div>
                <div class="section-title white">{{ setting('shared.video.title') }} <span>{{ setting('shared.video.title_colored') }}</span> {{ setting('shared.video.title_after') }}</div>
            </div>
        </div>
        <div class="row">
            <div class="text-center col-md-12">
                <a class="vid" href="{{ setting('shared.video.url') }}">
                    <div class="vid-butn"> <span class="icon"> <i class="ti-control-play"></i> </span> </div>
                </a>
            </div>
        </div>
    </div>
</section>
